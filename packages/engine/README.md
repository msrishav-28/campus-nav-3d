# @campus-nav/engine

Three.js scene engine — handles:
- Loading `.glb` building models
- Floor visibility toggling
- Raycasting for room click/tap selection
- Path rendering (tube mesh along room centroids)
- Camera animation (fly-to on room select)
